import { StyleSheet } from 'react-native';

import globalStyles from '/styles';

const styles = StyleSheet.create({
  content: {
    width: '100%',
    flexDirection: 'row',
    paddingTop: globalStyles.padding.xss,
  },
});

export default styles;
