import { StyleSheet } from 'react-native';
import { constants } from '/component/ListItem';

export default StyleSheet.create({
  image: {
    width: constants.iconSize,
    height: constants.iconSize,
  },
  checkboxStyle: {
    paddingLeft: 0,
  },
});
