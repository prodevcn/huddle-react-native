import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    textAlign: 'center',
    width: 245,
    // todo toadums this is just a placeholder to position the text until we have an image
    marginTop: 100,
  },
});
