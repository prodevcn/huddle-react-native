import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    flex: 1,
  },
  childWrapper: {
    flex: 1,
  },
  emptyList: {
    justifyContent: 'flex-start',
  },
});
