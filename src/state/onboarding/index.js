import * as actionImport from './onboarding.actions';
import * as selectorImport from './onboarding.selectors';

export { default } from './onboarding.reducer';

export const actions = actionImport;
export const selectors = selectorImport;
